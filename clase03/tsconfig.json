{
  // Visit https://aka.ms/tsconfig to read more about this file
  "compilerOptions": {
    // File Layout
    "rootDir": "./src",      // ✅ CAMBIO: Descomentado - Define carpeta de archivos fuente TypeScript
    "outDir": "./dist",      // ✅ CAMBIO: Descomentado - Define carpeta de archivos JavaScript compilados

    // Environment Settings
    // See also https://aka.ms/tsconfig/module
    "module": "nodenext",
    "target": "esnext",
    // For nodejs:
    "lib": ["esnext"],       // ✅ CAMBIO: Descomentado - Habilita APIs modernas de JavaScript (for...of, ??, ?.)
    "types": ["node"],       // ✅ CAMBIO: Descomentado - Incluye definiciones de tipos de Node.js (console, setTimeout, etc.)

    // and npm install -D @types/node

    // Other Outputs
    "sourceMap": true,
    "declaration": true,
    "declarationMap": true,

    // Stricter Typechecking Options
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,

    // Style Options
    // "noImplicitReturns": true,
    // "noImplicitOverride": true,
    // "noUnusedLocals": true,
    // "noUnusedParameters": true,
    // "noFallthroughCasesInSwitch": true,
    // "noPropertyAccessFromIndexSignature": true,

    // Recommended Options
    "strict": true,
    "jsx": "react-jsx",
    "verbatimModuleSyntax": true,
    "isolatedModules": true,
    "noUncheckedSideEffectImports": true,
    "moduleDetection": "force",
    "skipLibCheck": true,
  }

  /*
  📋 RESUMEN DE CAMBIOS REALIZADOS EN ESTE ARCHIVO:
  
  ✅ CAMBIOS REALIZADOS:
  1. "rootDir": "./src"     - Descomentado para definir carpeta de fuentes TypeScript
  2. "outDir": "./dist"     - Descomentado para definir carpeta de archivos compilados
  3. "lib": ["esnext"]      - Descomentado para habilitar APIs modernas de JavaScript
  4. "types": ["node"]      - Descomentado para incluir tipos de Node.js
  5. Eliminado "types": []  - Removido duplicado que causaba conflicto
  
  🎯 PROPÓSITO DE LOS CAMBIOS:
  - Organizar proyecto con estructura src/ y dist/ profesional
  - Habilitar sintaxis moderna de JavaScript (nullish coalescing ??, optional chaining ?.)
  - Permitir uso de APIs de Node.js (console.log, setTimeout, etc.)
  - Resolver errores de compilación con configuración estricta
  
  ⚠️ CONFIGURACIONES ESTRICTAS MANTENIDAS:
  - "exactOptionalPropertyTypes": true  - Requiere tipos opcionales explícitos
  - "noUncheckedIndexedAccess": true   - Valida acceso seguro a arrays/objetos
  - Todas las demás opciones estrictas del archivo original
  
  💡 ANTES: Archivos .js se compilaban junto a los .ts en carpeta raíz
  💡 DESPUÉS: Estructura organizada con src/ para fuentes y dist/ para compilados
  */
}
